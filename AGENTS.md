# Codex 작업 규칙

## 먼저 읽을 문서

1. [docs/PROJECT_CONTEXT.md](docs/PROJECT_CONTEXT.md): 실제 코드 구조, 실행 후보, 알려진 문제.
2. [docs/FINAL_RESULTS.md](docs/FINAL_RESULTS.md): 결과의 증거 수준과 사용할 수 있는 수치.
3. [docs/WORK_LOG.md](docs/WORK_LOG.md): 이전 Codex 작업과 재개 순서.
4. [RESTORE_WINDOWS.md](RESTORE_WINDOWS.md): 원본 백업 복원 방법.
5. [docs/MIGRATION_WINDOWS.md](docs/MIGRATION_WINDOWS.md): 최신 환경 기록·보충 백업과 Windows 재개 순서.

## 원본·결과 보존

- Raw experimental data를 임의 수정·삭제·이름 변경·덮어쓰기 하지 않는다. CSV/TXT, 센서 기록, 입력 경로, 기존 결과 그림·요약, 학습 데이터, 모델 가중치도 보존 대상이다. 결측 보정이나 필터링이 필요하면 별도 파생 파일을 만들고 원본 SHA-256과 처리 조건을 기록한다.
- 기존 분석 스크립트는 실행만으로 기존 결과를 덮어쓸 수 있다. 출력 경로를 먼저 확인하고 새 실행별 폴더를 사용한다. 경로 생성기가 `planned_path.txt`, `feature_points.txt` 등을 쓰는지도 확인한다.
- 결과 수치를 추측하지 않는다. 코드의 주기·해상도·임계값은 설정값이며, 측정 FPS·정확도·안전성·성공률로 바꾸어 쓰지 않는다. 자료가 없으면 `UNKNOWN`, 다음 확인 작업은 `TODO`로 표기한다.
- 보고할 수치는 입력 파일/해시, 코드 버전, 실행 인자, 단위, 시간 구간, 필터, 집계식, 반복 수와 연결한다. 조건이 다른 결과를 하나의 표에서 동일 기준의 우열로 해석하지 않는다.
- `FINAL_RESULTS.md`에 명시된 증거 수준을 지킨다. 파일명에 final/최종/v최대번호가 있거나 README에서 main이라고 부른다는 이유만으로 최종 실험으로 인정하지 않는다.

## 최종·중간·debug 구분

- 최종: 연구자가 채택한 실험과 입력/코드/조건/산출물 대응이 확인된 경우만 지정한다. 미확정이면 후보로 남긴다.
- 중간·과거: `oldversion/`, `old_version/`, `archive/`, 이전 번호 파일과 복구된 버전. 필요할 때 비교하되 삭제하거나 자동으로 최신 실행 경로에 덮어쓰지 않는다.
- debug/실패 참고: `debug*`, `fails/`, `*_patched*`, 임시 파일 등은 실제 역할을 읽고 분류한다. patched라는 이름만으로 현재 빌드에 사용된다고 가정하지 않는다.
- 새 실험은 기존 결과 경로 밖에 만들고 README/실행 메타데이터로 기준선·실험·debug를 표시한다.

## 코드 변경 전후

- 먼저 `git status`, 현재 커밋, 실행 진입점, 토픽 이름/메시지 타입/QoS, 좌표계·단위·관절 순서, 입력/출력 파일을 확인한다. 기존 동작을 설명할 수 있을 때 최소 변경을 한다.
- 정상 동작의 근거가 있으면 기준선으로 보존한다. 확인되지 않은 상태를 '원래 정상'이라고 단정하지 않는다. 변경 전에 기존 파일을 보존하고 변경 전후 차이를 검증한다.
- 이 저장소의 로봇·카메라 코드를 일반 단위 테스트처럼 실행하지 않는다. 장치 제어·파일 쓰기 등 부작용을 먼저 확인한다. 이미 승인된 범위의 읽기·분석·문서 작업은 불필요한 재승인 없이 수행한다.
- 검증하지 않은 하드웨어 동작·시뮬레이션 성공을 주장하지 않는다. 테스트 종류와 한계를 WORK_LOG에 남긴다.
- README와 기존 인수인계 문서는 검증 대상이다. 설명이 실제 코드/결과와 다르면 우선 불일치를 기록한다. README만 믿고 실행 파일·실험 수치·최종 버전을 확정하지 않는다.
- 보존용 Git 이력, 중첩 프로젝트, 타 연구 자료를 현재 실행 시스템과 구분한다. Git 설정/인증 토큰/개인 키를 출력하거나 커밋하지 않는다. 원본 모델 백업의 비공개 설정을 유지한다.
- 사용자 지시가 이 문서보다 우선한다. 문서만 요청받은 작업에서 코드 수정·결과 재생성·장치 구동으로 범위를 넓히지 않는다.

## 이 저장소에서 특히 주의할 점

저장소 이름: `vision`. 기준 커밋과 구체적인 실행 후보는 docs/PROJECT_CONTEXT.md를 확인한다. 최종 인정 여부가 UNKNOWN이면 연구자가 확인하기 전까지 그대로 유지한다.
양손 제스처 입력과 두 로봇 팔 제어를 구분한다. ROS 1 reference와 현재 ROS 2 토픽을 혼합하지 않는다. central의 surface 경로 문제가 알려져 있으므로 README만 보고 즉시 정상 실행을 가정하지 않는다.
